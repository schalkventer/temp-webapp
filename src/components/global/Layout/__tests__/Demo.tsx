import React from 'react';

import Layout from '../index';

const Demo = (): JSX.Element => (
  <Layout>
    <h1>This is an h1 tag wrapped inside of a Layout</h1>
  </Layout>
);

export default Demo;
