import React from 'react';

import Layout from '../index';

const Default = (): JSX.Element => (
  <Layout>
    <h1>This is an example of an H1 tag wrapped inside of Layout Component</h1>
  </Layout>
);

export default Default;
